import React, { Component } from 'react';

export default class Books extends Component {

  render() {
    // const { items } = this.props;

    return (
      <p>
        I am a page!
      </p>
    );
  }
}